# ROS2 Senses Package Python Dependencies
# Install in venv to avoid system package conflicts
catkin_pkg
empy
lark-parser
setuptools
wheel

# Audio processing
pyaudio>=0.2.13
soundfile>=0.12.1
sounddevice>=0.4.6
# Using playsound 1.2.2 (1.3.0 is broken on Python 3.12+)
playsound==1.2.2

# Speech recognition & synthesis
openai-whisper>=20231117
piper-tts>=1.2.0

# ML/AI dependencies
numpy>=1.24.0
onnxruntime>=1.16.0

# Networking
requests>=2.31.0

# GPIO (if not already installed system-wide)
# gpiozero is typically installed via apt, but include for completeness
# gpiozero>=2.0

# Note: librosa is imported conditionally in ears.py for resampling
# Only needed if audio sample rate != 16kHz
librosa>=0.10.0

# AI Agents
strands-agents[bidi,bidi-io]>=1.41.0
strands-agents-tools

boto3
aws-sdk-bedrock-runtime>=0.6.0

# Acoustic echo cancellation / noise suppression for robot voice loop
pywebrtc-audio>=0.1.0
